name: Cron probe

# TEMPORARY DIAGNOSTIC. The `scan` workflow's schedule has never fired.
# This is the minimal possible scheduled workflow - no inputs, no concurrency,
# no permissions block - to isolate whether the problem is scan.yml's config
# or the repository's schedule registration. Delete once answered.

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "probe fired at $(date -u +%H:%M:%SZ) via ${{ github.event_name }}"
